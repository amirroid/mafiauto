CREATE TABLE Player (
  id INTEGER NOT NULL PRIMARY KEY,
  name TEXT NOT NULL
);

getAllPlayers:
SELECT * FROM Player;

addPlayer:
INSERT INTO Player(name) VALUES (?);

deletePlayer:
DELETE FROM Player WHERE id = ?;